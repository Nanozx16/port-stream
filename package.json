 "version": "2.0.0",Add commentMore actions
  "description": "A module for creating a node style stream over a WebExtension port object.",
  "main": "dist/index.js",
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "build": "rm -rf ./dist && tsc --project .",
    "lint": "eslint . --ext ts,js,json",
